    <header class="header">
        <section class="header__inner flex">
            <p class="header__name txtBold">
                <span class="mr16">{{ $user->shop->name }}</span>
                {{ $user->roll->name}}
            </p>
        </section>
    </header>